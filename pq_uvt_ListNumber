let
  // Give me a list of CountValue numbers starting at StartValue, with an increment of IncValue for each subsequent number.
  CountValue = 20,
  StartValue = .1,
  IncValue = .1,
  List = List.Numbers(StartValue,CountValue,IncValue)
in
    List
